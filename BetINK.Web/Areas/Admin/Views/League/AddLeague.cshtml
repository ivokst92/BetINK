@model LeagueFormViewModel
@{
    ViewData["Title"] = "Add new league";
}

<h2>@ViewData["Title"]</h2>
<div class="row">
    <div class="col-md-4">
        @Html.Partial("_LeagueFormModel", Model);
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}